import Main from "../../components/templates/institute/Main";

function Institute() {

  return (
    <div className="w-full">
      {/* <Table columns={columns} rows={data?.data || []} /> */}
      <Main/>
    </div>
  );
}

export default Institute;
